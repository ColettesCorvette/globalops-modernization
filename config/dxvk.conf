# Plafond framerate : la physique de LithTech est cadencee sur les FPS.
# Sans plafond, la detection de collision laisse traverser les murs.
dxvk.maxFrameRate = 120

# Compteur FPS + version DXVK en surimpression.
# Pour le retirer : commenter la ligne ci-dessous.
dxvk.hud = fps,version
